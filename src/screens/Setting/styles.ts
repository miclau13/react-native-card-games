import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 32
  },
  title: {
    marginBottom: 10,
    textAlign: 'center',
  },
});

export default styles;